import { ProfileProps } from 'interfaces/common'
import React from 'react'

const Profile = ({
  type,
  name,
  avatar,
  email,
  properties
}: ProfileProps) => {
  return (
    <div>Profile</div>
  )
}

export default Profile