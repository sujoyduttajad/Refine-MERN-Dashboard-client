import { AgentCardProp } from "interfaces/agent";

const AgentCard = ({
  id,
  name,
  email,
  avatar,
  noOfProperties,
}: AgentCardProp) => {
  return <div>AgentCard</div>;
};

export default AgentCard;
